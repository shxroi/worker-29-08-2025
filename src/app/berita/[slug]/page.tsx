export default async function BeritaDetailPage({ params }: { params: { slug: string } }) {
    const { slug } = params;


    return (
        <div>
            <h1>asda</h1>
        </div>
    );
}

